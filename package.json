{
  "name": "message-network",
  "version": "0.0.1",
  "description": "Highly scalable, modular, flexible, lightweight and performant solution for evented interactions.",
  "main": "dist/index.js",
  "scripts": {
    "test": "_mocha --require babel-register --recursive test",
    "retest": "babel-watch `npm bin`/_mocha -- --compile js:babel-register --recursive test",
    "cover": "istanbul cover `npm bin`/_mocha -- --require babel-register --recursive test",
    "compile": "babel -d dist lib",
    "prepublish": "npm run compile"
  },
  "keywords": [
    "message",
    "messages",
    "network",
    "events",
    "EventEmitter"
  ],
  "author": "rmdm",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-core": "^6.18.2",
    "babel-plugin-transform-runtime": "^6.15.0",
    "babel-preset-es2015": "^6.18.0",
    "babel-register": "^6.18.0",
    "babel-watch": "^2.0.3",
    "istanbul": "^1.1.0-alpha.1",
    "mocha": "^3.1.0",
    "sinon": "^1.17.6"
  },
  "dependencies": {
    "babel-runtime": "^6.18.0"
  }
}
